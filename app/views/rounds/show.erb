<p><b>Correct on first try: </b><%= (session[:round_info][:correct].round) - (session[:round_info][:incorrect_cards].count.round) %></p>
<p><b>Out of: </b><%= Deck.find(session[:round_info][:deck_id]).cards.count %></p>
<p><b>Correct%: </b><%= ((((session[:round_info][:correct]).to_f - (session[:round_info][:incorrect_cards].count)).to_f / (Deck.find(session[:round_info][:deck_id]).cards.count).to_f)*100).round %>%</p>
<p><b>Total guesses this round: </b><%= session[:round_info][:guesses] %></p>
<br>
<p>Would you like to <b><a href="/sessions/new">Sign In</a></b> or <b><a href="/users/new">Register</a></b> to save your round?</p>
